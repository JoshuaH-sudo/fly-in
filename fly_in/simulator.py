"""Simulation skeleton for Fly-in."""

from fly_in.models import Network


def run_simulation(network: Network) -> list[str]:
    """Return empty turn output for now."""
    _ = network
    return []
