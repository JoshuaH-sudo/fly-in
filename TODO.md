# Algorithm
- [] Need to detect if end is isolated from the start zone
- [] Make sure to not get stuck in a loop.
- [] Need to handle waiting and zone capacity to prevent drones from colliding.
- [] Need to handle the case where a drone is waiting in a zone and another drone is trying to enter the same zone. The waiting drone should be able to enter the zone if there is capacity, otherwise it should wait until there is capacity.